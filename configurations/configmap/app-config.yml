apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
# pipe operator is used to provide multi line value
  app.metadata: |
    version=12.0.234
    author=|
      "mohit.malhotra@barco.com"
      "ron.mackenzy@abc.com"
    flavour=lite
    supportedRegion=IN,CN,US
